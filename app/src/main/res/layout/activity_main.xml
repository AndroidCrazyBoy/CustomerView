<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.baolx.customerview.MainActivity">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <Button
                android:id="@+id/custom_switch"
                style="@style/main_btn_style"
                android:text="自定义控件开关" />
            <Button
                android:id="@+id/custom_slide"
                style="@style/main_btn_style"
                android:text="带有侧滑的item" />
            <Button
                android:id="@+id/custom_bottombar"
                style="@style/main_btn_style"
                android:text="可上滑的底部菜单栏" />
            <Button
                android:id="@+id/custom_process"
                style="@style/main_btn_style"
                android:text="文字带有进度的进度条" />
        </LinearLayout>
    </ScrollView>
</RelativeLayout>
